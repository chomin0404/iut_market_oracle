# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.env

# uv
.python-version

# Local Claude settings (personal overrides — not shared)
.claude/settings.local.json
# Transient hook state (regenerated on each session)
.claude/state/
# Audit logs (generated at runtime)
.claude/audit/config-changes*.jsonl*

# App artifacts (SQLite DB, runtime outputs)
artifacts/

# Jupyter
.ipynb_checkpoints/

# Type checker / linter caches
.mypy_cache/
.pyright/
.ruff_cache/

# Test caches
.pytest_cache/

# Separate modeling_api project (root-level, separate venv — not part of this package)
# Keep excluded to prevent import namespace collision with src/modeling_api/
modeling_api/

# Local working outputs
output/

# OS
.DS_Store
Thumbs.db
